# PostgreSQL connection – copy to .env and fill in your values
# Use double underscore __ for .NET config key: ConnectionStrings__DefaultConnection

ConnectionStrings__DefaultConnection=Host=localhost;Database=notesdb;Username=postgres;Password=postgres;Port=5432
